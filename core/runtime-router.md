# EMPIRA Runtime Router

> **EMPIRA component**
> Component ID: `runtime-router`
> Version: `1.0.0`
> Included in: `EMPIRA 0.2.0`

Use this text in a host's persistent project or global instructions when the host cannot install the router programmatically.

## Canonical router

For any work with EMPIRA, first find and read the current `Memory System Guide` in the user's Notion. It is the canonical source of global Memory rules.

If the request involves preparing, recording, or retrieving information, find and follow the matching current workflow: `Prepare for Memory`, `Capture to Memory`, or `Recall from Memory`.

If correct interpretation requires object types, domains, statuses, properties, or object boundaries, also read the current `Memory Schema Reference`.

Do not treat agent memory, earlier chats, project instructions, local skills, cached copies, or a GitHub distribution snapshot as canonical when they conflict with the current documents in Notion.

Cached page IDs may be used only as navigation hints. Before relying on them, verify that the page still exists, has the expected Component ID, and read its current content.

Within one session, do not reread already verified current documents unless needed.

## Placement rule

Prefer project-level instructions when EMPIRA should apply only inside a dedicated project. Use global/custom instructions only when the user explicitly wants EMPIRA routing across all chats and understands that it will affect unrelated conversations.
